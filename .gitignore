# PyInstaller-Ausgabe (die fertige exe ist ~89 MB und wird aus dem Quellcode gebaut)
build/
dist/
dist_new/

# Wird beim Bauen aus models.APP_VERSION erzeugt (siehe amboss.spec).
# Mitversioniert stand hier zuletzt 1.0.0, waehrend die Anwendung bei 1.2.2 war.
version_info.txt

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Laufzeitdaten - liegen eigentlich in %APPDATA%\AV1VideoConverter,
# hier nur als Sicherheitsnetz, falls mal etwas im Projektordner landet
*.log
config.json
